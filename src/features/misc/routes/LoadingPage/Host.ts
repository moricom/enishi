import { styled } from "styled-components";

export const LoadingPageHost = styled.div`
    min-height: calc(100vh - 7rem);
    height: 100%;
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
`;
